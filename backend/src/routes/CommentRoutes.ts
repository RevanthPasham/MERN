// import { Router } from "express";
// import {
//   addComment,
//   getComments,
//   getSummary,
// } from "../controllers/commentController";
// import { authMiddleware } from "../middlewares/auth";

// const router = Router();

// // POST /api/comments/:productId
// router.post("/:productId", authMiddleware, addComment);

// // GET /api/comments/:productId
// router.get("/:productId", getComments);

// // GET /api/comments/summary/:productId
// router.get("/summary/:productId", getSummary);

// export default router;
